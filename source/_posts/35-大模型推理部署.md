---
title: 大模型推理部署
date: 2025-04-26 10:17:21
description: 大模型推理部署相关记录
cover: /covers/35.webp
tags:
    - inference
    - llm
categories:
    - 推理部署
---

### KV cache

> 参考资料：[大模型推理加速：看图学KV Cache](https://zhuanlan.zhihu.com/p/662498827)

 KV cache 只能用于decoder架构，这是因为Decoder有Causal Mask,在推理的时候前面已经生成的字符不需要和后面的字符产生attention, 从而使得前面已经计算的K和V可以缓存起来。

可以看看上面那个链接的图！！！很清晰了



### Nano-vllm

> https://github.com/GeeeekExplorer/nano-vllm
>
> 解析的文档：[2025最快下手vLLM的项目-nanovllm源码解读](https://zhuanlan.zhihu.com/p/1925484783229698084)



### MNN-LLM

#### python和C++的绑定

- 直接使用Python提供的C API (#include <Python.h>) 这是Python官方提供的标准方式，用于创建C/C++扩展模块，并没有使用pybind11或者其它的库

在导出MNN模型时(针对LLM模型哈)

```Python
## 位置：transformers/llm/export/llmexport.py

def export(**):

## 通过不断的链接，这个函数中将 llm.tensor ==> llm.mnn 转换的实现主要是由C++进行实现的
## python库的名称是 _tools
## 文件顺序 llmexport.py ==> llm/export/utils/mnn_converter.py ==> MNN/tools/mnnconvert.py
```

具体的C++/python绑定库在pymnn/pip_package/setup.py 这个文件夹，在这个地方产生了 _mnncengine这个库和_tools这个库

```Python
    ################################################################################
    # Declare extensions and package
    ################################################################################
    extensions = []
    packages = find_packages()
    engine = Extension("_mnncengine",\
                    libraries=engine_libraries,\
                    sources=engine_sources,\
                    language='c++',\
                    extra_compile_args=engine_compile_args + extra_compile_args,\
                    include_dirs=engine_include_dirs,\
                    library_dirs=engine_library_dirs,\
                    extra_link_args=engine_link_args + extra_link_args\
                        + make_relative_rpath('lib'))
    extensions.append(engine)
    tools = Extension("_tools",\
                    libraries=tools_libraries,\
                    sources=tools_sources,\
                    language='c++',\
                    extra_compile_args=tools_compile_args + extra_compile_args,\
                    include_dirs=tools_include_dirs,\
                    library_dirs=tools_library_dirs,\
                    extra_link_args=tools_link_args + extra_link_args\
                        + make_relative_rpath('lib'))
    extensions.append(tools)
```

#### 跨平台的实现

不同平台的实现代码在 source/backend, 里面可以看到很多后端的实现，如opengl，Vulkan，metal (使用计算着色器实现算子)， qnn

#### 初始化阶段

1. **创建Interpreter实例**：
   1. 使用`Interpreter::createFromFile()`或`Interpreter::createFromBuffer()`加载模型
   2. 模型数据会被解析并存储在Content结构中
2. **创建Session**：
   1. 调用`Interpreter::createSession()`创建会话
   2. 根据配置选择合适的后端（CPU、NNAPI、OpenCL等）

#### LLM的demo

首先按照文档进行文档的转换，然后按照/pymnn/examples/MNNLlm

其中create函数调用是在下面两个地方 (python接口是 create, 实际调用底层C++函数是createLLm)

```C++
// 文件位置：pymnn/src/llm.h

static PyObject* PyMNNLLM_create(PyObject *self, PyObject *args) {
    if (!PyTuple_Size(args)) {
        return NULL;
    }
    const char* path = NULL;
    int embedding_model = 0;
    if (!PyArg_ParseTuple(args, "s|p", &path, &embedding_model)) {
        return NULL;
    }
    LLM *llm = (LLM *)PyObject_Call((PyObject*)&PyMNNLLM, PyTuple_New(0), NULL);
    if (!llm) {
        return NULL;
    }
    if (embedding_model) {
        llm->llm = MNN::Transformer::Embedding::createEmbedding(path);
        llm->is_embedding = true;
    } else {
        llm->llm = MNN::Transformer::Llm::createLLM(path);
    }
    return (PyObject*)llm;
}
```

其对应的C++部分实现是：

```C++
// 文件位置：transformers/llm/engine/src/llm.cpp

Llm* Llm::createLLM(const std::string& config_path) {
    std::shared_ptr<LlmConfig> config(new LlmConfig(config_path));
    Llm* llm = nullptr;
    if (config->is_visual() || config->is_audio() || config->has_talker()) {
        llm = new Omni(config);
    } else {
        llm = new Llm(config);
    }
    return llm;
}
```



### 参考资料

1. [读懂 AI Infra，看这六个关键词就够了](https://mp.weixin.qq.com/s/bBrXZrwaeHIVg5_lR02Ddg)

   
