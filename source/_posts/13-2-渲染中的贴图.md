---
title: 渲染中的贴图
date: 2023-03-05 15:04:23
tags:
    - OpenGL
---

### 贴图文件介绍

贴图文件按格式大致分为3种：

- 没有压缩的图片格式
- CPU压缩的图片格式
- 显卡支持的图片格式

在Windows系统中，不压缩的图片格式就是 .bmp，下面用画图软件创建1024x1024的图片，保存为.bmp格式。

![img](https://quvideo.feishu.cn/space/api/box/stream/download/asynccode/?code=MmI1MGQ5ZGExZmQwZDIxMWNkNGRiZGIyZmI5MDJjMmFfT2NYRVcxdVY3djJ5VW1GdDZpTUVBMlA0dkk3dDJNTFpfVG9rZW46Ym94Y25RZUNqT0Y1T2tsVWUwc0duWDZGRUlMXzE3NzI2OTQzMTI6MTc3MjY5NzkxMl9WNA)

### **CPU压缩的图片**

在日常工作中，几乎不会用到.bmp格式，因为占用空间太大了，存储/传输都不方便。

对于大文件，我们一般会用文件压缩软件对其进行压缩，保存为.zip等格式。

文件压缩方式分为无损压缩和有损压缩。

源文件压缩之后，再解压得到的文件，和源文件一致，这就叫做无损压缩。

我们熟知的.zip .rar .7z都是无损压缩，毕竟数据完整性是硬性需求。

- 无损压缩

.png是常见的无损压缩格式。

- 有损压缩

用的较多的有损压缩是 .jpg。

**显卡支持的图片格式**

我们编写代码，读取.png、.jpg图片文件用于渲染，流程如下：

![img](https://quvideo.feishu.cn/space/api/box/stream/download/asynccode/?code=YTIyZmFlN2U1YTRlZDBiYWM5MzI0NmJiNTAyZGFjN2ZfUVY0YlpaMGdBUmNRUjBtN2s3YmYxSTZEbzFvaVlrOWtfVG9rZW46Ym94Y25RcEFiSEFiN1BnRTd1RTF2clU3WE5jXzE3NzI2OTQzMTI6MTc3MjY5NzkxMl9WNA)

因此一般我们都会选择将.png、.jpg图片，预先转换为显卡支持的图片格式，打包到游戏安装包，这样进游戏就只需要加载到内存，然后上传到GPU即可。

![img](https://quvideo.feishu.cn/space/api/box/stream/download/asynccode/?code=YjAyOWM2Y2Y4YzhkNzMzNjJmYjM0YzU4YzJhMjM4NWVfOGdwVXE1Z2prYXB2cm9ncEdpd2l0eXlIYlFGVkhaeXVfVG9rZW46Ym94Y25pem0zTnJJZ2pkcjFncks1VWpOM29iXzE3NzI2OTQzMTI6MTc3MjY5NzkxMl9WNA)

### CPU和GPU的通信方式

常用的.png图片，需要解压得到RGB数据，并且上传到GPU中。注意这里用上传这个词，而不是复制，这是由CPU和显卡的通信方式决定的。

CPU与GPU的通信方式

CPU和GPU是一种CS模式，即客户端-服务器模式。客户端不能直接访问服务器资源，客户端想对服务器资源进行操作，只能通过网络协议交互，由服务器进行操作。

CPU就是客户端，GPU就是服务器。

### 关于图片的优化

针对图片，为什么会耗时：

- 解析jpg慢：jpg是一种压缩图片文件，需要解压才可以得到未压缩的RGB数据，解压是很慢的一个过程。
- 压缩慢：对RGB数据进行压缩为GPU直接支持的格式，压缩需要耗费时间。
- 上传慢：4096的图片，即使压缩后，上传要耗费一定时间。

对应的解决办法就是：使用GPU支持的压缩纹理数据格式。

具体做法就是：将压缩好的纹理数据，保存到硬盘作为图片文件。

带来的好处是：

- 无需解析，直接上传GPU。
- 数据已经压缩，无需再次压缩。
- 数据已经压缩，上传数据量小。

OpenGL提供了接口glGetCompressedTexImage，将压缩好的纹理数据，从显存下载到内存

### 参考资料

https://github.com/ThisisGame/cpp-game-engine-book
